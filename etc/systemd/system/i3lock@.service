[Unit]
Description=Lock screen before suspend
Before=sleep.target

[Service]
User=%I
PAMName=systemd-user
Type=forking
Environment=DISPLAY=:0
ExecStart=-/bin/bash -c 'exec $HOME/bin/lock.sh'

[Install]
WantedBy=suspend.target
